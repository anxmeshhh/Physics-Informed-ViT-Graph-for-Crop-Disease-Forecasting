# Core deep learning stack.
# torch/torchvision/torchaudio must be the CUDA build and must agree in version:
#   pip install torch==2.2.2+cu121 torchvision==0.17.2+cu121 torchaudio==2.2.2+cu121 \
#       --index-url https://download.pytorch.org/whl/cu121
torch==2.2.2
torchvision==0.17.2
torch_geometric==2.5.3

# Vision transformers (DINOv2, ViT, Swin, DeiT)
transformers==4.41.2

# captum >= 0.8 requires torch >= 2.3 and will silently upgrade torch,
# breaking the CUDA build. Keep it pinned.
captum==0.7.0
shap>=0.46

numpy<2
pandas>=2.0
scikit-learn>=1.3
pyarrow>=14
matplotlib>=3.8
pillow>=10
requests>=2.31
pyyaml>=6
tqdm>=4.66

# API + dashboard
fastapi>=0.110
uvicorn[standard]>=0.29
python-multipart>=0.0.9
